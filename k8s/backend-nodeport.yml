apiVersion: v1
kind: Service
metadata:
  name: portfolio-backend-nodeport
spec:
  selector:
    app: portfolio-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30500     # Accessible sur http://localhost:30500
  type: NodePort
